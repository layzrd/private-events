<p id="notice"><%= notice %></p>

<p>
  <strong>Creator:</strong>
  <%= @event.creator.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @event.description %>
</p>

<ul>
  <% @event.attendee.each do |guest| %>
    <li><%= guest.name %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
